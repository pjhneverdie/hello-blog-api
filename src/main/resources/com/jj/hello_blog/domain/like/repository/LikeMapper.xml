<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jj.hello_blog.domain.like.repository.LikeMapper">

    <insert id="saveLike" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `like`(member_id, post_id, comment_id)
        VALUES (#{memberId}, #{postId}, #{commentId})
    </insert>

    <select id="countLikeById" resultType="int">
        SELECT COUNT(*)
        FROM `like`
        WHERE post_id = #{postId}
        <if test="commentId != null">
            AND comment_id = #{commentId}
        </if>
    </select>

</mapper>