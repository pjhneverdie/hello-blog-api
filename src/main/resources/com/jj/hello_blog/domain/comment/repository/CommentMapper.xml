<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jj.hello_blog.domain.comment.repository.CommentMapper">

    <insert id="saveComment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comment(content, member_id, post_id, parent_id)
        VALUES (#{content}, #{memberId}, #{postId}, #{parentId});
    </insert>

    <update id="updateComment">
        UPDATE comment
        SET content = #{content}
        WHERE id = #{id};
    </update>

    <delete id="deleteComment">
        DELETE
        FROM comment
        WHERE id = #{id};
    </delete>

    <select id="findCommentById" resultType="Comment">
        SELECT id, content, created_at, member_id, post_id, parent_id
        FROM comment
        WHERE id = #{id}
    </select>

</mapper>